# Gesture Beauty Cam

Webcam beauty filters — slim face, bigger eyes, soft glow — controlled with hand gestures.
A single self-contained HTML file, built with MediaPipe FaceMesh + Hands.

## Run

Open `beauty.html` in a browser and allow camera access.
Some browsers only enable the camera over `https://` or `localhost`.
